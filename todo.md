1.在本地找不到对应情况时没有联网搜索功能。

2.追问的依据应该是对命中的文档的聚类算法，按照分类进行提问来缩小范围。

3.生成向量库时用的是embeddinggemma:latest模型，而且感觉scripts文件夹下的生成向量库的脚本很复杂。
考虑先使用自己编写的几个数据作为demo，验证基本功能。

4.上下文管理不善，不能只看两轮，应该进行合理的压缩，只保留关键内容。

5.没有回话的保存和召回功能，在设计可视化UI时应该考虑到。

6.api启动有问题，localhost:8000反应404.

7.normalize阶段只调用模型分析用户意图不稳妥，可能存在幻觉和不清晰（如头疼：headache+怎么办:management）